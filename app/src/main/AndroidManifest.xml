<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.anthonydevs.intento3.pasteleria">

    <!-- 🔒 Permiso para conectarse a Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Intento3">

        <!-- Actividad principal (Inicio/Login o pantalla inicial) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad del catálogo -->
        <activity
            android:name=".CatalogoActivity"
            android:exported="false" />

        <!-- Actividad del Registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Actividad del Cuenta -->
       <activity android:name=".CuentaActivity" />

        <!-- Actividad del Cuenta -->
        <activity android:name=".CarritoActivity" />


   </application>

</manifest>
